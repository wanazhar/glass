---
id: ir-030-024
scope: issue-30-mcp-compile-task-documentation
status: done
depends-on: [ir-030-023]
---

# Objective

Document the browser-free MCP `compileTask` workflow for agent clients.

# Contract

- The guide shows a valid JSON-RPC `tools/call` request and typed response.
- It states that compilation validates semantic tasks without Chrome startup,
  mutation leases, selectors, or input-value echoing.
- It distinguishes compile output from execution; the plan is not a browser
  action result.

# Verification

- documentation build and repository checks pass;
- examples match the implemented MCP tool and Task Protocol schema.
