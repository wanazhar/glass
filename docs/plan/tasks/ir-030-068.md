---
id: ir-030-068
scope: task-execution
status: complete
depends-on: [ir-030-067]
release: 0.2.7
---

# Field-read redaction and revision hardening

## Delivered

- Added fixture-backed coverage proving password values are returned as
  `<redacted>` under the default sensitive-value policy.
- Added a post-observation revision check so a field read cannot report values
  from a page that changed while form controls were being inspected.

## Verification

- Linux ARM64 browser smoke verifies both empty-field state and password
  redaction through `field.read`.
- Full Rust, lint, documentation, package, and release metadata checks remain
  required before publication approval.
