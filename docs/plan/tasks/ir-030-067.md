---
id: ir-030-067
scope: task-execution
status: complete
depends-on: [ir-030-066]
release: 0.2.7
---

# Guarded semantic field reading

## Delivered

- `field.read` now resolves one semantic form target within the caller's
  revision-scoped region and reads its bounded current form state.
- The result exposes value, checked, selected option, empty, read-only, and
  required metadata through the existing structured extraction contract.
- Existing form-value policy enforcement and password/payment redaction remain
  authoritative; missing inputs, ambiguous targets, stale revisions, and
  unavailable controls fail closed.

## Verification

- Fixture-backed Linux ARM64 browser smoke executes `field.read` through the
  canonical dispatcher and verifies the empty-state result.
- Full Rust, lint, documentation, package, and release metadata checks remain
  required before publication approval.
