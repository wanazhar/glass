# graph-035-009 — typed development evidence and replay convergence

Status: Complete locally on 2026-08-11. No remote mutation performed.

The shared tool-router boundary now projects bounded call/result metadata into
typed repository, file, symbol, editor revision, Git change, process, port, LSP,
DAP, browser, Web IR, workflow, test, kernel, agent, task, experiment, and tool
nodes plus observable subsystem replay events.

`glass.graph.link` records an explicit typed relationship only between existing
nodes, under authenticated actor provenance and mutation confirmation. Temporal
order is never relabeled as causality. Path/explain operations can therefore
return evidence paths without private reasoning.

The deterministic test populates every required resource kind, records
`file -> Git change -> test -> Web IR regression`, verifies exact relations and
provenance, and asserts replay contains subsystem events with no private
rationale.
