# Final review: mcp-008 hardened MCP transport

Reviewed branch through `33b57fb` (`fix: validate MCP envelopes before
dispatch`).

Conclusion: **pass**. No blocking findings remain for `mcp-008`.

## Resolution

The main loop now validates `jsonrpc == "2.0"` before dispatching either
`notifications/cancelled` or `notifications/initialized`
(`src/mcp/server.rs:227-241`, `266-270`). Invalid notifications remain silent
and exit without touching lifecycle or cancellation state. Requests with an id
still receive the bounded `-32600` envelope error.

The lifecycle integration sends an invalid initialized notification between
negotiation and a pre-ready `tools/list`, proving it does not transition the
server; only the subsequent valid notification enables tools
(`tests/browser_smoke.rs:143-185`). The real-browser cancellation integration
sends an invalid cancellation notification while a request is active, asserts
that no cancellation response arrives, then confirms a valid notification
cancels that same request (`tests/browser_smoke.rs:282-307`).

This closes the sole P2 blocker from `mcp-008-02` without changing framing,
request ownership, response redaction, concurrency, or valid lifecycle paths.

## Focused verification

The branch's prior full validation and real-browser record were reused.
Independent inexpensive checks passed:

- `cargo test mcp::server::tests --lib` — 14 passed;
- `cargo test --test browser_smoke mcp_enforces_initialization_lifecycle_and_notification_silence -- --exact` — 1 passed;
- `git diff --check 85f528d 33b57fb`.

No implementation changes were made during this review.
