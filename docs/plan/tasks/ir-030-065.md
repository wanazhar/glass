---
id: ir-030-065
scope: task-execution
status: complete
depends-on: [ir-030-064]
release: 0.2.7
---

# Bounded semantic collection extraction

## Delivered

- `collection.extract` now executes through the guarded task runtime for a
  uniquely scoped semantic collection region.
- The result uses the existing structured extraction contract with bounded
  repeated target records and `$.targets` provenance.
- Non-collection scopes fail closed during preflight instead of silently
  treating arbitrary page content as a collection.

## Verification

- Fixture-backed Linux ARM64 browser smoke extracts two semantic result links
  through the canonical CLI/MCP-compatible Rust dispatcher.
- Full Rust, lint, documentation, package, and release metadata checks remain
  required before publication approval.
