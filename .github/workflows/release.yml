name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: glass-linux-x86_64
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact: glass-macos-x86_64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: glass-macos-aarch64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: glass-windows-x86_64
          - os: windows-latest
            target: i686-pc-windows-msvc
            artifact: glass-windows-i686
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build release
        run: cargo build --release --locked --target ${{ matrix.target }}

      - name: Run tests
        run: cargo test --all-targets --locked --target ${{ matrix.target }}

      - name: Run Linux browser smoke
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        run: GLASS_E2E=1 cargo test --test browser_smoke --locked -- --nocapture

      - name: Package artifact
        shell: bash
        run: |
          mkdir -p dist
          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            cp target/${{ matrix.target }}/release/glass.exe dist/${{ matrix.artifact }}.exe
            sha256sum dist/${{ matrix.artifact }}.exe > dist/${{ matrix.artifact }}.sha256
          else
            cp target/${{ matrix.target }}/release/glass dist/${{ matrix.artifact }}
            strip dist/${{ matrix.artifact }}
            sha256sum dist/${{ matrix.artifact }} > dist/${{ matrix.artifact }}.sha256
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/${{ matrix.artifact }}${{ matrix.os == 'windows-latest' && '.exe' || '' }}
          if-no-files-found: error

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}-checksum
          path: dist/${{ matrix.artifact }}.sha256
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: glass-*
          path: dist
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: dist/*
