---
id: ir-030-066
scope: task-execution
status: complete
depends-on: [ir-030-065]
release: 0.2.7
---

# Bounded semantic table extraction

## Delivered

- `table.extract` now executes through the guarded task runtime for a uniquely
  scoped semantic table region.
- The result uses the existing structured extraction contract with bounded table
  target records and `$.targets` provenance.
- Non-table scopes fail closed during preflight rather than being interpreted as
  tabular data.

## Verification

- Fixture-backed Linux ARM64 browser smoke extracts the Orders table through the
  canonical Rust dispatcher.
- Full Rust, lint, documentation, package, and release metadata checks remain
  required before publication approval.
