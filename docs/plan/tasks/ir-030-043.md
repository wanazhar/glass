# ir-030-043

Status: complete

## Objective

Extend the checked-in Glass protocol golden fixture with successful typed
responses for browser-free Web IR and Task Protocol operations.

## Contract

- Golden responses use the canonical `webIr.*` and `task.validate` operation
  request IDs.
- Successful result payloads decode through the typed Rust response helpers.
- Existing browser and typed-error envelopes remain unchanged.
- Fixture JSON stays bounded and deterministic.

## Evidence

- `protocol_conformance` round-trips every fixture envelope.
- The conformance test dispatches canonical operation IDs to typed response
  decoders and checks representative result fields.
