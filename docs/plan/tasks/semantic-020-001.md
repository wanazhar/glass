---
id: semantic-020-001
scope: versioned semantic observation contract
status: completed
depends-on: []
---

# Objective

Define the canonical bounded semantic observation envelope before adding
classification or interface behavior.

# Delivered

Added version-one Rust contracts for semantic page kinds, region kinds,
confidence, route-scoped expansion handles, evidence, and omission limits.
Canonical JSON is validated for bounds, duplicate region IDs, route identity,
and revision-scoped handles. The external schema is published at
`docs/schema/semantic-observation-v1.schema.json`.
