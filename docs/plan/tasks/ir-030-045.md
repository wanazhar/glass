# ir-030-045

Status: complete

## Objective

Advertise the canonical Task Protocol and draft Web IR schema versions through
Glass capability negotiation.

## Contract

- Capability manifests expose `task: [1]` and `webIr: [1]` schema entries.
- Negotiation accepts exact version requests for both schemas.
- CLI discovery and MCP initialization expose the same schema inventory and
  negotiated `task: 1` and `webIr: 1` selections.
- Existing protocol, action, workflow, and capability status behavior remains
  unchanged.
- The advertised contracts are browser-free and do not imply native extraction
  or cross-platform browser certification.

## Evidence

- Capability manifest tests assert both schema entries.
- Negotiation tests request and assert both selected versions.
- Protocol conformance asserts CLI/MCP public manifest parity and negotiated
  schema selections.
- MCP and schema-compatibility documentation show the new request shape.
