# glass-browser

`glass-browser` is the standalone Glass Browser Intelligence Runtime. It owns
browser control, structured semantic observation, workflows, verification,
CLI, MCP, daemon, and Rust APIs without requiring the full development shell.

```bash
cargo install glass-browser
glass-browser --help
```

The umbrella `glass` command is provided separately by `glass-dev`.
